import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ScheduleWrapper = () => {
  return (
    <View>
      <Text>ScheduleWrapper</Text>
    </View>
  );
};

export default ScheduleWrapper;

const styles = StyleSheet.create({});
